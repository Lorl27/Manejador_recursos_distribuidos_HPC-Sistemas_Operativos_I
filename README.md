# Librerias necesarias 
```console
sudo apt install build-essential manpages-dev  (si estamos en debian/ubuntu)
sudo apt install libc6-dev
```


# Ejecutar
./agente 192.168.1.50 8100 "cpu:4 mem:8192 gpu:1"